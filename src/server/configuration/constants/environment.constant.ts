export enum Environment {
  Development = 'development',
  Test = 'test',
  Production = 'production',
}

export enum EnvironmentFile {
  development = '.env',
  test = '.test.env',
  production = '.prod.env',
}
